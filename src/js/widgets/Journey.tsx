import React from 'react'
import ReactDOM from 'react-dom/client'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <div> Hello there traveller! How is your journey going so far... </div>
  </React.StrictMode>
)
